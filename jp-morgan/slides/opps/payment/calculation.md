# Calculating Payments

- users send hashes of ID || date (96 bits of entropy)
- BU sends all collected hashes to WashU
- WashU checks all combinations of users in list and dates and sees which exist in the set

<SlideCurrentNo class="absolute bottom-8 right-10"/>