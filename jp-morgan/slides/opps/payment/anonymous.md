# Computing Anonymous IDs

- users send hashes of ID || date (96 bits of entropy)
- WashU sends list of all possible hashes for anonymized ID numbers
- BU aggregates data from same anonymized ID (allows for aggregation over time)

<SlideCurrentNo class="absolute bottom-8 right-10"/>